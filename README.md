# SecureChat
SecureChat is a simple protocol designed to allow people to send end to end encrypted messages anonymously across a distributed network.
By allowing anybody to host servers and join networks of servers, groups can send messages to each other without worrying about having their private information stolen.
An exhaustive copy of the protocol is currently being written.
